-- Create the database for Global Primary Learning Hub
CREATE DATABASE IF NOT EXISTS GPL;
USE GPL;

-- Tables are auto-created by the server on startup (initSchema),
-- but you can run this file manually if you prefer.
-- Example:
--   mysql -u root -p < setup_db.sql
